[project]
name = "tbuddies-decomp"
version = "0.0.0"
description = "Team Buddies matching decompilation"
requires-python = ">=3.11"
dependencies = [
    "splat64[mips]>=0.50.0,<1.0.0",
    "mcp-proxy>=0.12.0,<1.0.0",
    "mcp>=1.17.0,<2",
]

[tool.uv]
package = false
